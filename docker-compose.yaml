version: "3.9"
services:
  ring-connector:
    build: .
    volumes: 
      - ./data:/app/data
      - ./oauth-autorization.json:/app/oauth-authorization.json
    environment: 
      - OAUTH_FILE=/app/oauth-authorization.json
      - DATA_DIR=/app/data